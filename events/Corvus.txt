add_namespace = COV

country_event = {
    id = cov.1
    title = cov.1.t
    desc = cov.1.d
    #picture = GFX_

    is_triggered_only = yes #focus
	fire_only_once = yes

    option = {
        name = cov.1.a
		trigger = {
		on_declare_war = {
		effect = {
				if = {
					limit = {
						tag = COV
						FROM = { tag = CET }
						}
						complete_national_focus = COV_initial_offensive
					}
				}
			}
		}
		mean_time_to_happen = {
        days = 2
		}	
        hidden_effect = {
            complete_national_focus = COV_initial_offensive
        }
    }
}